<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Genesis">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.example.Genesis.menu.Account.Person"></activity>
        <activity android:name="com.example.Genesis.menu.Account.Account" />
        <activity android:name="com.example.Genesis.menu.FriendsList.Friend" />
        <activity android:name="com.example.Genesis.menu.FriendsList.FriendsList" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCyby5CgdNevoEkTeUPla9-37ZW4qjB2wg" />

        <activity
            android:name="com.example.Genesis.menu.Map.Map"
            android:label="@string/title_activity_map"/>

        <activity
            android:name="com.example.Genesis.menu.Map.DemoMap"
            android:label="@string/title_activity_map"/>


        <activity android:name="com.example.Genesis.menu.Register.Register" />
        <activity android:name="com.example.Genesis.menu.Account.Setting" />
        <activity android:name="com.example.Genesis.menu.Account.Inventory.view.InventoryMainActivity" />
        <activity android:name="com.example.Genesis.menu.Account.Inventory.view.EquipmentActivity" />
        <activity android:name="com.example.Genesis.menu.Account.Inventory.view.EquipmentAdapter" />
        <activity android:name="com.example.Genesis.menu.Account.Inventory.view.PlayerInventory" />
        <activity android:name="com.example.Genesis.menu.Quests" />
        <activity android:name="com.example.Genesis.menu.Account.planner.Planner" />
        <activity android:name="com.example.Genesis.menu.Account.Status" />
        <activity android:name="com.example.Genesis.menu.MainMenu.MainMenu" />
        <activity android:name="com.example.Genesis.menu.Login.Login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>